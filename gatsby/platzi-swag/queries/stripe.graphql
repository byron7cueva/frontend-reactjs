query {
  allStripePrice {
    edges {
      node {
        id
        unit_amount
        unit_amount_decimal
        type
        product {
          name
          metadata {
            description
            img
            wear
          }
        }
      }
    }
  }
}
