# Informacion del sitio
# siteMetadata viene desde el archivo de configuracion de gatsby
query {
  allSite{
    edges{
      node{
        siteMetadata{
          description
        }
      }
    }
  }
}